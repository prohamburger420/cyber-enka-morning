# -*- coding: utf-8 -*-
"""合成の設定の**正本**（2026-09-05）。

★なぜ作ったか
  同じ設定が3箇所に独立して存在していて、**実際に食い違った**:
    generate_v2.py の REF_MAP        … talk1, news
    deploy/runner/tts.py REF_BY_CORNER … talk1 だけ
  2026-09-05 に nordw が「ふざけたニュースをよんでるこずえは1段階テンションあげよう」と
  指示して参照音声を差し替えたが、**本番(Actions)側には入っていなかった**。
  ローカルで耳判定して詰めた設定が本番で再現されないなら、耳判定の意味がなくなる。

★置き場所の違いは**ディレクトリだけ**なので、ここでは**ファイル名と書き起こし**を持つ。
  各側が自分のディレクトリと結合する:
    ローカル  … kozue-asa/voice/ref/
    本番      … R2から取った assets/ref/

⚠ 参照wavと書き起こしは**必ず対**。書き起こしが欠けると、モデルが辻褄合わせに
  合成文の冒頭を食う（2026-09-04 実測: 「おはようございます！」が5回中5回消えた）。
⚠ 参照は3〜10秒でないとGPT-SoVITSがエラーになる。
"""

# ---- 合成パラメータ（2026-09-04 nordw耳判定で確定）----
SPEED = 1.10
#   ★speed_factorは"発話速度"ではなく**生成後のタイムストレッチ**。
#     下げると引き伸ばし臭が出て、nordwが即検知する。
FRAGMENT_INTERVAL = 0.5
#   ★"間"はこちらで作る。「早口すぎる」と感じたら速度でなく間を疑う。
SPLIT_METHOD = "cut2"
#   ★cut5(読点ごと)には戻さないこと。読点のたびに別々に生成して貼り合わせるので
#     **断片ごとに抑揚がリセットされる**＝流暢でない構造的な原因だった。
#     cut2(約50字ごと)なら文がひとまとまりで生成され、文中の抑揚が繋がる。
#     副産物: 生成時間が実測 42.5秒→23.6秒に半減。

# ★seedを固定する（2026-09-05）。既定は -1 ＝**毎回ランダム**で、
#   同じ台本でも毎回違う音が出ていた＝**A/B比較の再現性がゼロ**だった。
#   「棒読みっぽい」「間が不自然」を今後いじるなら、ここが固定でないと何を比べても分からない。
#   値そのものに意味は無い。ローカルと本番で**同じ値**であることだけが大事。
SEED = 20260905

# ★★seedを渡すだけでは足りない（2026-09-05 実測）。
#   `parallel_infer=True` だと**同じseedでも結果が揺れる**。実測:
#     parallel=True  … 2回の出力が不一致。音声長も 19.3秒 / 19.9秒 とばらつく
#     parallel=False … 2回ともバイト一致
#   ⚠ 速度の代償は無かった（36.4秒 vs 32.3秒＝むしろ切ったほうが速い）。
#     CPU推論なので並列にしてもコアの取り合いになるだけ。
#   ★これが無いと「棒読みっぽい」「間が不自然」を触っても、
#     変わったのが設定のせいか揺らぎのせいか区別がつかない。
PARALLEL_INFER = False

# ---- 参照音声（＝喋り方）----
#   GPT-SoVITSでは音色は重みに焼いてあるので、**喋り方は参照音声が決める**。
#   コーナーごとに差し替えることで「OPだけ元気に」といった出し分けができる。
REF_DEFAULT = ("RECITATION324_004.wav",
               "ハイチ共和国でツーサンルーベルテュールが勝利を収められたのは"
               "実際大熱病のおかげだった")

REF_BY_CORNER = {
    # OPは元気に（2026-09-04 nordw耳判定。EMOTION100から5案聴き比べて014）
    "talk1": ("EMOTION100_014.wav",
              "スミスさん、ピエール・デュボワをご紹介しますわ。私の親友なの。"),
    # 電脳芸能ニュースはテンションを一段上げる（2026-09-05 nordw指示）
    # ⚠ 手元の参照音声は2本だけ。ITAコーパス本体がディスクに無いので、
    #   これ以上の細かい段階分けができない。刻みたければコーパスを取り直して耳で選ぶ。
    "news": ("EMOTION100_014.wav",
             "スミスさん、ピエール・デュボワをご紹介しますわ。私の親友なの。"),
}


def resolve(ref_dir) -> dict:
    """ファイル名を、渡されたディレクトリ配下の実パスに解決して返す。

    返り値は {コーナー名: (Path, 書き起こし)}。既定は "" のキーに入れる。
    """
    from pathlib import Path
    d = Path(ref_dir)
    out = {"": (d / REF_DEFAULT[0], REF_DEFAULT[1])}
    for k, (f, t) in REF_BY_CORNER.items():
        out[k] = (d / f, t)
    return out
